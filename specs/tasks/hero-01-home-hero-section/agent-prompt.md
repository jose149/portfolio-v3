# Executor handoff

Task: `hero-01-home-hero-section`

Execution mode: `checkpointed`

## Read

1. `AGENTS.md`
2. `specs/agents/executor.md`
3. `specs/tasks/hero-01-home-hero-section/spec.md`
4. `specs/tasks/hero-01-home-hero-section/tasks.md`
5. Skills listed in `spec.md`
6. Affected `apps/web` files

## Goal

Implement the homepage Hero section in `apps/web` following the approved design documentation.

## Do

- implement only the accepted task;
- execute checkpoints in order;
- use the required skills listed in `spec.md`;
- follow `spec.md` for scope, requirements and acceptance criteria;
- follow `tasks.md` for checkpoints and verification;
- update `result.md` with checkpoint progress and validation evidence.

## Do not

- implement a full header or navigation redesign;
- implement Projects, About, Experience or Contact sections;
- add Sanity integration, analytics, Tailwind or unrelated dependencies;
- perform broad refactoring;
- claim unexecuted checks passed.

## Verification

Use the commands and manual checks listed in `tasks.md`.

## Report

Return:

- checkpoint completed;
- files changed;
- verification results;
- deviations or blockers;
- remaining checkpoints.
